This project uses the following languages / frameworks / libraries:
- Python v3.14
- Django 6.0.X

Always activate the virtual environment before running any python commands -- `source venv/bin/activate`
